---
name: java-spring-helper
description: 自动优化 Spring Boot 代码结构，生成样板代码。
---
当编写 Java 代码时，自动激活。
1. 检查是否使用了 `@Autowired` 构造器注入（推荐）而非字段注入。
2. 确保 `@Service` 和 `@Controller` 注解完整。
3. 自动生成 Lombok `@Data` 或 `@Builder` 注解。
